# 术语表

## 1. 简介

本文档用于统一皮带偏移检测系统中算法、业务相关专业名词的定义，消除项目组内部理解歧义，确保各成员在需求沟通、算法开发、测试验收等环节对同一名词的理解保持一致。本文档覆盖需求列表（需求列表.md）中全部功能需求（FR）描述所涉及的专业名词，供全体组员查阅。本文档不新增需求文档之外的自定义概念，所有定义均与需求文档描述保持一致。

## 2. 术语定义

| 名词          | 定义                                                                                   |
| ----------- | ------------------------------------------------------------------------------------ |
| 皮带          | 输送系统中用于承载和运输物料的环形带状部件，是本系统检测偏移行为的对象。                                                 |
| 皮带区域        | YOLO 实例分割模型在视频图像中识别出的皮带所在区域，为后续边缘提取、跑偏计算提供基础数据。                                      |
| 皮带轮廓        | 皮带在图像中呈现的整体外形边界，由 YOLO 实例分割模型输出，用于表征皮带的走向与位置。                                        |
| 皮带边缘        | 皮带轮廓两侧的边界线，基于 YOLO 实例分割输出的皮带轮廓、借助 OpenCV 做轮廓处理后解析轮廓坐标信息得到，分为左边缘与右边缘，是计算中线、判断偏移的关键特征。 |
| 基准中线        | 采集皮带正常工况图像／视频，提取皮带边缘计算得到的正常运行中线，作为后续跑偏判定的基准参考线。                                      |
| 当前皮带运行中线    | 根据皮带左右边缘坐标计算单处中线点，利用多组中线点拟合得到的完整中线，用于与基准中线对比以判断跑偏。                                   |
| 跑偏角度        | 对比当前皮带中线与基准中线，计算得到的两条直线的夹角；夹角超过阈值即判定存在跑偏趋势。                                          |
| 像素偏移距离      | 当前皮带相对基准位置的最大像素偏移距离，以像素为单位。                                                          |
| 实际偏移距离      | 结合皮带像素宽度、实际物理宽度完成像素‑实际距离换算后得到的物理空间偏移量，计算公式为：实际偏移距离 = 像素偏移距离 ÷ 皮带像素宽度 × 实际皮带宽度。       |
| 像素‑实际距离换算   | 利用皮带像素宽度与实际物理宽度之间的对应关系，将像素偏移距离转换为实际偏移距离的换算方法。                                        |
| 皮带像素宽度      | 图像中皮带所占的像素宽度，是像素‑实际距离换算的输入参数之一。                                                      |
| 实际皮带宽度      | 皮带在物理空间中的实际物理宽度，是像素‑实际距离换算的输入参数之一。                                                   |
| 跑偏角度阈值      | 可配置的判定阈值，当跑偏角度超过该阈值时判定存在跑偏趋势或皮带跑偏。                                                   |
| 跑偏距离阈值      | 可配置的判定阈值，当偏移距离超过该阈值时判定皮带跑偏。                                                          |
| 跑偏判定        | 当偏移角度**或**偏移距离超出对应阈值时，判定皮带跑偏，并由界面输出报警提示。                                             |
| 跑偏报警        | 跑偏判定成立后，界面输出的报警提示。                                                                   |
| YOLO 实例分割   | 一种基于 YOLO 框架的实例分割算法，用于识别视频内皮带区域，输出皮带所在区域、皮带轮廓、模型推理置信度，为后续边缘提取、跑偏计算提供基础数据。            |
| OpenCV 轮廓处理 | 基于 YOLO 实例分割输出的皮带轮廓，借助 OpenCV 对轮廓进行处理并解析轮廓坐标信息，用于提取皮带左、右边缘位置坐标。                      |
| 模型推理置信度     | YOLO 实例分割模型输出皮带区域时给出的置信度，用于表征识别结果的可信程度。                                              |
| 基准配置        | 采集皮带正常工况图像／视频并设置基准中线的过程，摄像头位置变动后可重新配置基准线以适配现场。                                       |
| 多帧综合判断      | 支持多帧综合判断，降低单帧图像抖动、噪声带来的误报警，属于抗误报能力的一部分。                                              |
| 实时推理检测      | 能够对视频流做实时推理检测，满足工业现场监控实时性要求。                                                         |
| 视频采集        | 系统读取摄像头或已有监控视频，连续获取皮带运行画面；支持工业实时摄像头、监控视频、本地视频文件多种输入；对视频逐帧处理，处理结果实时展示在系统界面。           |
| 可视化参数配置界面   | 提供可视化参数配置界面，支持配置基准中线、皮带实际物理宽度、跑偏角度阈值、跑偏距离阈值。                                         |
| PyQt 图形界面   | 系统采用的图形界面框架，划分视频显示区、参数配置区、检测结果区、告警提示区，操作人员可快速完成参数配置与状态查看。                            |
| 功能扩展        | 系统架构预留扩展空间，后续可扩展皮带撕裂、异物、堆料等其他皮带故障检测。                                                 |
| 环境适应性       | 适配工业场景不同光照、背景、轻微遮挡；摄像头位置变动后可重新配置基准线适配现场。                                             |
